#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../glob@10.3.4/node_modules/glob/dist/cjs/src/bin.js" "$@"
else
  exec node  "$basedir/../../../../../glob@10.3.4/node_modules/glob/dist/cjs/src/bin.js" "$@"
fi
